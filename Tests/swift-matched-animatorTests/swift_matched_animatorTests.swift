import XCTest
@testable import swift_matched_animator

final class swift_matched_animatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_matched_animator().text, "Hello, World!")
    }
}
